# DREDGE Title Screen Command

Adds a terminal command to toggle the title screen UI.

To use the command just open the terminal with the key \` and type in `title 0` to disable or `title 1` to enable the title screen UI.
